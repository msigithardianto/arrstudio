<?php
// app/views/pages/docs/sections/output.php
?>
<section class="docs-section" data-doc="output">
  <h1 data-anchor="output">Output Files <a class="anchor" href="#output">#</a></h1>
  <p>Lima jenis output yang bisa kamu download.</p>

  <h2 data-anchor="out-script">1. Behavior Script (.lua)</h2>
  <p>Script handler untuk tombol toggle, close, dan interaksi. Paste ke <code>LocalScript</code> di <code>StarterPlayerScripts</code>.</p>

  <h2 data-anchor="out-full">2. Full Lua (.lua)</h2>
  <p>Script lengkap yang build seluruh UI dari nol. Cocok untuk <code>LocalScript</code> standalone.</p>

  <h2 data-anchor="out-rbxmx">3. RBXMX (.rbxmx)</h2>
  <p>File XML untuk import langsung ke Roblox Studio via <strong>File → Import</strong>.</p>

  <h2 data-anchor="out-plugin">4. Plugin (.lua)</h2>
  <p>Plugin untuk auto-import UI. Setelah install, toolbar ARRR Studio muncul di Studio.</p>

  <h2 data-anchor="out-tree">5. Tree</h2>
  <p>Struktur hierarki Instance dalam format text. Berguna untuk debug.</p>
</section>